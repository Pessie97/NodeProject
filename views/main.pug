doctype html
html
  head
    title #{title}
    link(rel='stylesheet', href='/css/stylesheet.css')
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    
      block header
          header.page_title
            img(src='/pictures/tnc_logo.png').logo
            |  #{title}
          ul.menu1
            
            li
              a(href=`/about`).
                Join
            li
              a(href = `/about`).
                About
            li
              a(href= `/classes`).
                Sign In
            li
              a(href= `/add_class`).
                Contact
      block content
      